# Concept 27

## Goal
Step-through VIN analysis with a playful progress bar.

## Tech
- HTML
- CSS
- Vanilla JS

## Features
- 3-step wizard
- Progress fill
- Confetti-less completion state

## How to run
Open `index.html` in a browser (double-click or via a static server). For concepts that load CDN assets (maps, charts, GSAP), use a simple static server so subresources load reliably:

```bash
npx serve .
```
