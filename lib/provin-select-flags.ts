/**
 * PROVIN SELECT (stratēģiskā konsultācija) — publiski atslēgts.
 * Pakalpojums vairs netiek piedāvāts; atstāts tikai, lai esošais kods nesalūztu.
 */
export function isProvinSelectPublic(): boolean {
  return false;
}
