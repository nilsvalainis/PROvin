# Concept 15

## Goal
Dark metal canvas with champagne gold accents and slow cinematic motion.

## Tech
- HTML
- CSS
- Vanilla JS

## Features
- Gold gradient accents
- Soft vignette
- Ambient drift animation

## How to run
Open `index.html` in a browser (double-click or via a static server). For concepts that load CDN assets (maps, charts, GSAP), use a simple static server so subresources load reliably:

```bash
npx serve .
```
