# Concept 12

## Goal
Oversized headline hierarchy with subtle kinetic typography.

## Tech
- HTML
- CSS
- Vanilla JS

## Features
- Clamp mega type
- Split-line motion
- Scroll-linked skew micro

## How to run
Open `index.html` in a browser (double-click or via a static server). For concepts that load CDN assets (maps, charts, GSAP), use a simple static server so subresources load reliably:

```bash
npx serve .
```
